{
  "name": "@expent/api",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "bacon dev",
    "check": "bacon check",
    "test": "bacon test",
    "clean": "rm -rf .turbo",
    "build": "cargo build --release"
  }
}
