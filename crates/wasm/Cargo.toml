[package]
name    = "wasm"
version.workspace = true
edition.workspace = true

[lib]
crate-type = [ "cdylib" ]

[dependencies]
chrono       = { features = [ "wasmbind" ], workspace = true }
rust_decimal = { workspace = true }
wasm-bindgen = "0.2.121"
